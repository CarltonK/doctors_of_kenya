import 'package:flutter/material.dart';

class AdminConcierge extends StatelessWidget {
  const AdminConcierge({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
