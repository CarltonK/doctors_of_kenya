import 'package:flutter/material.dart';

class AdminStore extends StatelessWidget {
  const AdminStore({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
