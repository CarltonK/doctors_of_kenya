import 'package:flutter/material.dart';

class AdminOpportunities extends StatelessWidget {
  const AdminOpportunities({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
